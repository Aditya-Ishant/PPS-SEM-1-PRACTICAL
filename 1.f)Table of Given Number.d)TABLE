#include <stdio.h>

void main() {
    int num, z;
    
    printf("Enter the number you want the table of: ");
    scanf("%d", &num);
    
    for(int i = 1; i <= 10; i++) {
        z = num * i; // Calculate the product
        printf("%d X %d = %d\n", num, i, z);
    }
}
