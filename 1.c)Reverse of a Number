#include <stdio.h>

void main() {
    int n;
    int rem, rev = 0;

    printf("Enter the number you want to reverse: ");
    scanf("%d", &n);

    while (n != 0) {
        rem = n % 10;
        rev = rev * 10 + rem;
        n /= 10;
    }

    printf("The reversed number is: %d\n", rev);
}
